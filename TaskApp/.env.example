# Copy this to TaskApp/.env and fill in values.
# API_PORT is where the Express mail API will listen
API_PORT=5174

# SMTP settings (required)
# Common examples:
# - Outlook/Hotmail consumer: SMTP_HOST=smtp-mail.outlook.com, SMTP_PORT=587
# - Microsoft 365 business:   SMTP_HOST=smtp.office365.com,   SMTP_PORT=587
# - Gmail (app password):     SMTP_HOST=smtp.gmail.com,       SMTP_PORT=587
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Optional. If omitted, the sender defaults to SMTP_USER.
MAIL_FROM=
